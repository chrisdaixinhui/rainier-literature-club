# 雨山前 Rainier Literature Society — 网站产品需求文档

> **文档版本**：v0.15<br>
> **创建日期**：2026-05-12<br>
> **更新日期**：2026-08-28<br>
> **产品阶段**：上线前验收<br>
> **当前状态**：网站主体、内容自动化与本地生产构建已完成；真实内容、第三方完整链路、发布工程与正式部署尚未完成<br>
> **负责人**：Chris Dai

---

## 1. 产品概览

### 1.1 背景

雨山前书会（Rainier Literature Society）是根植于西雅图的中文阅读社群。网站承担品牌展示、活动发现、社群连接、邮件订阅、票务承接与长期内容归档等职责，并通过低代码内容后台降低日常运营成本。

### 1.2 核心价值主张

> 在英语世界里，给中文热爱一个栖居之所。

首页当前进一步使用以下表达强化品牌定位：

> 我们在异乡，用中文重新彼此相认。<br>
> Meet again in Chinese.

### 1.3 产品目标

| 目标 | 说明 | 当前衡量能力 |
|---|---|---|
| 品牌展示 | 建立可信、有文学气质且可被分享的线上门面 | 已有完整页面；尚无品牌认知指标 |
| 活动增长 | 帮助用户发现并报名雨山前及友社活动 | 已有活动流与报名外链；尚无点击埋点 |
| 社群沉淀 | 通过邮件与微信形成可持续触达渠道 | Mailchimp 与微信入口已接入；尚无转化看板 |
| 内容运营 | 让运营者无需改代码即可维护活动与每日一句 | Notion CMS 已接入 |
| 内容资产 | 将往期活动沉淀为可归档、可分类浏览的社群档案 | 活动归档、分类筛选、海报画廊与 Footer 每日一句已实现；全文搜索未实现 |
| 商业支持 | 承接票务、周边与捐赠 | 票务结构已实现；周边与捐赠未上线 |

### 1.4 目标用户

**主要用户**：居住在西雅图及周边地区、年龄约 20–45 岁的中文使用者；希望保持中文阅读习惯、参与线下文化活动或结识同好。

**次要用户**：对中文文化感兴趣的非华裔人士、远程关注社群动态的海外华人、友社组织者与潜在支持者。

### 1.5 设计原则

| 原则 | 要求 |
|---|---|
| 诗意克制 | 留白、纸张质感与编辑排版优先，避免通用商业模板感 |
| 内容优先 | 活动海报、照片与文字是主体，界面只承担组织和引导 |
| 双语共存 | 中英文同屏呈现，不设置语言切换，不要求逐句机械翻译 |
| 移动优先 | 核心访问、浏览、弹窗与外链操作必须在手机端成立 |
| 低门槛运营 | 活动、票务、海报和每日一句优先由 Notion 维护 |
| 可降级 | 外部内容源失败时网站仍可打开，但降级必须可观测且内容可用 |
| 无障碍与减少动态 | 键盘、焦点、语义标签和 `prefers-reduced-motion` 应覆盖关键交互 |

---

## 2. 当前实现扫描摘要

### 2.1 功能范围

| 范围 | 状态 | 当前实现 |
|---|---:|---|
| 首页 `/` | ✅ | 分层山景 Hero、宣言、关于我们、社群照片、滚动展开的服务内容、雨山前与友社近期活动滚动展示、往期海报画廊 |
| 活动页 `/activities` | ✅ | 雨山前近期活动、友社推荐、往期活动分类筛选与详情展开 |
| 支持页 `/support` | ⚠️ | 周边占位与双档票务已实现；捐赠能力未上线，当前无公开捐赠入口 |
| 全局导航 | ⚠️ | 桌面端为 Logo + 订阅 CTA；页面导航链接仅在移动全屏菜单中；移动菜单状态语义与滚动锁定待补 |
| 全局订阅 | ✅ | 导航与 Hero 入口、明信片样式 Modal、邮箱校验、Mailchimp 双重确认请求 |
| 全局 Footer | ✅ | 品牌与页尾导航、社交链接、微信小助手、每日一句、动态年份 |
| 内容后台 | ✅ | Notion 活动库与每日一句库 |
| 海报持久化 | ✅ | Notion 文件转存 Cloudinary、URL 回写、缓存刷新 |
| 自动归档 | ✅ | 按状态或活动结束时间自动转为往期 |
| 部署 | ⏳ | 尚未完成正式 Vercel 部署与线上验收 |
| 自动化测试 | ⏳ | 当前没有单元、集成或端到端测试文件 |
| 数据分析 | ⏳ | 当前没有访问、报名、订阅或捐赠转化埋点 |

本次扫描基线为 `main` 分支提交 `04d6483`。扫描范围包含全部应用源码、配置、静态数据、依赖与部署配置，并对 Notion 做只读全量分页核对；同时执行 lint、生产构建、3 个页面与 3 个 API 的本地验证，以及 1440px 桌面端和 375px 移动端关键交互检查。

### 2.2 2026-08-28 内容现状

本地环境变量已完整配置，可只读访问 Notion。当前线上内容库状态如下：

| 内容 | 当前数量 | 说明 |
|---|---:|---|
| 活动记录 | 32 | 30 条“已完成”，2 条“即将举行” |
| 海报永久 URL | 29 | 3 条无海报 |
| 友社活动 | 1 | 当前为测试活动 |
| 置顶活动 | 1 | 当前为测试活动 |
| 有报名链接的活动 | 1 | 当前指向 `example.com` 测试链接 |
| 有票价的活动 | 1 | 当前为测试票价 |
| 启用的每日一句 | 76 | 已达到首批 50–100 条目标 |
| `example.com` 源链接 | 3 | 雨山前报名、友社详情、往期回顾各 1 个；票务两档复用雨山前报名 URL |

当前仍公开存在 3 条测试内容：

- `测试 · 友社活动`：2026-10-01，无海报；
- `测试 · 秋季读书会`：2026-09-20，Notion 无永久海报 URL，但代码以 `public/images/upcoming-test-poster.png` 临时覆盖；含测试报名链接与测试票价；
- `测试 · 雨山前Talk Vol.0`：已归档，含测试回顾链接。

### 2.3 质量验证结果

| 验证项 | 结果 |
|---|---|
| `npm run lint` | 通过，零错误 |
| `npm run build` | 通过，TypeScript 与生产构建成功；依赖侧出现非阻塞的 Node `DEP0205` 弃用警告 |
| 页面路由 | `/`、`/activities`、`/support` 均返回 200 |
| API 防护 | 未授权刷新与同步请求均返回 401 |
| 订阅校验 | 非法邮箱返回 400 与中文错误信息 |
| 桌面端浏览器 | 1440×900 下首页、活动页、支持页无横向溢出，图片无加载失败 |
| 移动端浏览器 | 375×812 下 Hero、CTA、滚动服务区、近期活动 Flow 布局与订阅 Modal 无横向溢出 |
| 关键交互 | 订阅 Modal 可聚焦邮箱、锁定滚动、关闭并归还焦点；海报详情和微信二维码 Dialog 可打开/关闭；往期活动可展开；移动菜单可打开并进入各页面 |
| 浏览器控制台 | 本地生产模式所检关键流程无错误或警告 |
| 自动化回归 | 缺失 |

生产构建的路由输出：

```text
/                    Partial Prerender，活动从 Data Cache 快照读取并按请求时间计算状态
/activities          Partial Prerender，活动从 Data Cache 快照读取并按请求时间计算状态
/support             Partial Prerender，活动从 Data Cache 快照读取并按请求时间计算状态
/api/revalidate      Dynamic
/api/subscribe       Dynamic
/api/sync-images     Dynamic
```

### 2.4 上线判断

**结论：代码可构建，核心体验可运行，但当前不满足正式上线条件。**

主要阻塞项：

1. Notion 中的近期活动、友社活动、票价和链接仍是测试数据；
2. Mailchimp 确认邮件完整链路尚未用真实测试邮箱验收；
3. 正式部署、Vercel 环境变量、Cron、域名和线上回归尚未完成；
4. 当前没有自动化回归测试，也没有外部服务失败监控；
5. 移动导航状态语义、背景滚动锁定和往期折叠正文的读屏隐藏尚未完成。

捐赠区块虽未上线，但此前失效的公开捐赠入口已从 Footer 移除，当前页面不会把用户导向不存在的 `#donate` 锚点。若重新上线捐赠入口，必须同时接入真实平台与完整说明。

### 2.5 开发进度确认

| 层级 | 进度 | 判断依据 |
|---|---:|---|
| 页面主体与核心交互 | 基本完成 | 3 个正式页面、全局导航/Footer、订阅与微信 Dialog、活动筛选/展开及首页滚动/轮播交互均已落地；导航与折叠语义仍有缺口 |
| 内容读取与运营自动化 | 已完成 | Notion 双库读取、分页、状态计算、持久快照、Cloudinary 转存回写、手动刷新与 Cron 接口已实现 |
| 第三方服务代码接入 | 已完成 | Notion、Cloudinary、Mailchimp 和 Vercel Cron 的代码与环境变量入口齐全 |
| 真实业务内容 | 未完成 | 唯一近期雨山前活动、唯一友社活动、唯一票务均为测试数据 |
| 第三方完整链路验收 | 部分完成 | Notion 只读访问与本地接口已验证；Mailchimp 确认邮件、Cloudinary 新图转存、线上 Cron 尚未做生产验收 |
| 发布工程 | 未完成 | 正式 Vercel 项目、环境变量、域名、HTTPS、线上回归与回滚方案无完成证据 |
| 质量保障 | 部分完成 | lint、TypeScript、生产构建和人工关键路径通过；自动化测试、限流、监控、完整无障碍与 SEO 尚缺 |

因此，当前应定义为“功能开发基本完成、上线准备未完成”，不能仅凭本地构建通过标记为已上线或可直接发布。

---

## 3. 信息架构与核心用户路径

```text
雨山前 Rainier Literature Society
│
├── 首页 /
│   ├── 品牌 Hero
│   ├── Manifesto 宣言
│   ├── Who We Are + 社群照片轮播
│   ├── What We Offer 滚动展开
│   ├── 雨山前 + 友社近期活动 Upcoming Showcase（有数据时显示）
│   └── 往期风采集 Event Gallery
│
├── 活动 /activities
│   ├── 雨山前近期活动
│   ├── 友社推荐（有内容时显示）
│   └── 往期活动 + 分类筛选 + 展开详情
│
├── 支持 /support
│   ├── 周边商品（占位）
│   └── 活动票务
│
└── 全局入口
    ├── 导航与 Hero 邮件订阅 Modal
    └── Footer
        ├── 页尾导航与社交媒体外链
        ├── 微信公众号与小助手二维码
        └── Quote of the Day
```

### 3.1 核心路径

| 用户意图 | 路径 | 成功条件 |
|---|---|---|
| 了解品牌 | 首页 Hero → 宣言 → 关于我们 → 社群照片 | 用户清楚“是谁、在哪里、做什么” |
| 找近期活动 | 首页近期活动或移动菜单 → 活动页近期区 → 报名或详情外链 | 真实活动、来源、时间、地点、海报与链接完整 |
| 浏览往期内容 | 首页画廊或活动页往期区 → 详情 | 能查看海报、分类、日期与完整介绍 |
| 订阅邮件 | 导航或首页 Hero → 输入邮箱 → 确认邮件 | Mailchimp 状态从 `pending` 变为 `subscribed` |
| 添加微信 | Footer 小助手 → 扫码或微信外链 | 二维码清晰、外链可打开 |
| 支持社群 | 支持页 → 票务或周边预告 | 票务链接真实有效；周边明确标注为未上线 |

---

## 4. 页面与交互需求

### 4.1 全局导航

**当前实现**：

- 固定于视口顶部，内页与移动端高度 64px；桌面首页按 `--home-hero-unit` 响应式缩放，Hero 可见时高度约 61–92px，离开 Hero 后缩小为约 47–70px，首页占位保持 Hero 高度；
- 左侧以暖白蒙版显示横版 Logo；
- 桌面端当前只展示 Logo 与订阅 CTA，没有首页、关于我们、活动板块、支持我们链接；
- 移动端展示 Hamburger，打开全屏菜单；
- 订阅 CTA 调用全局 `ModalContext`；
- 导航固定使用深森林绿背景，首页根据 Hero 可见性平滑调整高度。

**验收要求**：

- 键盘可聚焦并操作所有链接和按钮；
- 打开移动菜单后不得误触背景内容；
- 打开移动菜单后锁定背景滚动；
- 路由切换后菜单自动关闭；
- Logo、菜单、订阅按钮在 375px 宽度下不重叠；
- 移动菜单打开状态应提供明确的按钮状态语义；当前缺少 `aria-expanded` 与 `aria-controls`；
- 业务需确认桌面端是否有意只保留订阅 CTA；若不是，应恢复核心页面导航链接。

### 4.2 首页

#### A. Hero

当前 Hero 为一屏编辑型分层山景构图，不再是 Canvas 雨效：

- 深森林绿背景；
- 顶部显示 `Seattle · Chinese Literature Society` 与西雅图坐标；
- 主标题的无障碍名称为“在雨山前，重逢中文”；视觉上“雨山前”使用 `public/images/hero-logo.png`，其余文字混合斜体与衬线强调；
- 右侧或下方叠加 `hero-mountain-background.png` 与 `hero-mountain-foreground.png` 两层蓝色网点山景；
- 桌面精细指针移动和页面滚动共同驱动山体轻量位移，单轴最大约 40px；
- CTA 为“加入我们 · Join Us”和“查看活动”；
- “加入我们”打开订阅 Modal，“查看活动”进入 `/activities`；
- 桌面端采用左右构图，移动端改为纵向排布；
- 文案与分层山景使用 CSS 入场动画，运行时视差使用 React Hooks + `requestAnimationFrame`；减少动态偏好下关闭入场动画与山体位移。

#### B. Manifesto

- 主文案：“我们在异乡，用中文重新彼此相认。”；
- 英文副句：“Meet again in Chinese.”；
- 使用大字号衬线体与充足留白，承担品牌情绪转场。

#### C. Who We Are

- 中文介绍、英文简介与 `FIELD NOTES` 文案；
- 4 张 Cloudinary 社群合影，3:2 轮播；
- 默认每 4 秒切换；桌面 Hover 暂停并显示前后按钮；
- 支持圆点跳转；Hover 暂停时支持左右方向键。

上线前应确认 4 张图片的使用授权与长期 Cloudinary URL。

#### D. What We Offer

当前为三条编辑列表：

1. 多元共读 / Diverse Reading；
2. 纯粹体验 / Pure Experience；
3. 精神重逢 / Kindred Spirits。

底部提供“查看全部活动”入口。

- 标准动态模式下使用 `250svh` 滚动场景与 `100svh` Sticky 画框；
- 三条内容随滚动进度依次展开，当前展开项获得剩余高度，说明文字同步淡入与上移；
- 字体加载完成和窗口尺寸变化后重新测量折叠高度；
- `prefers-reduced-motion: reduce` 下取消 Sticky、动态高度与淡入，回退为三条完整静态列表。

#### E. Upcoming Showcase

- 存在雨山前或友社近期活动时渲染；顺序为雨山前活动在前、友社活动在后；
- 每张活动卡依次执行进入、停留、退出三个滚动阶段；
- 区块高度按活动数计算，当前为 `(活动数 × 3 + 1) × 100svh`；
- 卡片展示海报、序号、标题、中英文信息、来源、日期、地点、可滚动正文和报名/详情动作；
- 海报按实际宽高比计算布局；当海报与正文无法安全并排时自动切换上下堆叠的 Flow 布局，375px 下已验证会进入该布局；
- 雨山前活动使用报名动作，友社活动使用“了解详情”；无 URL 时显示对应的不可点击状态；
- 无海报时显示品牌占位；
- 非当前卡片使用 `aria-hidden` 与 `inert` 禁止误聚焦；正文区域支持键盘滚动；
- `prefers-reduced-motion: reduce` 下取消 Sticky 动画，改为静态卡片列表。

上线要求：不得展示标题含“测试”的活动或 `example.com` 链接。

#### F. Event Gallery

- 数据来自分类中的往期活动，要求有海报 URL；
- 按日期由新到旧排序，并按海报 URL 去重；
- 海报交替分入上下两排，等速反向无缝循环；
- Cloudinary 根据 240 / 360 / 480 / 640px 生成响应式缩略图；
- 桌面 Hover 暂停，单张卡片回正并轻微放大；
- 点击真实活动海报打开原生 `<dialog>`；
- Dialog 的详情卡在视口内居中，展示完整比例海报、分类、标题、日期和完整简介；
- 支持遮罩、关闭按钮、Escape、滚动锁定和焦点归还；
- 900px 以下改为海报在上、文字在下；
- 减少动态偏好下停止自动循环并允许手动横向浏览。

当 Notion 不可用或没有合格海报时，组件回退到 8 张内置 Cloudinary 海报。静态回退只用于展示，不打开活动详情。

#### G. Quote of the Day

- 当前位于全局 Footer，不再是首页主体的独立 Section；
- 读取 Notion 句库中“是否启用”为真的记录；
- 当前有 76 条启用记录；
- 展示中文、可选英文翻译、作者和出处；
- 缓存 10 分钟；
- 当前使用服务端运行日期生成每日索引。

需明确每日切换时区。产品建议统一为 `America/Los_Angeles`，避免 Vercel UTC 日期导致西雅图下午提前换句。

### 4.3 活动页

#### A. 雨山前近期活动

- 数据为非友社且状态为 `upcoming` 或 `coming_soon` 的活动；
- 按置顶优先、日期由近到远排序；
- 每条使用 3:4 满幅海报与同宽正文；
- 展示来源、状态、标题、日期、时间、地点、中英文简介与报名按钮；
- 正文完整读取 Notion，不自动摘要；
- 无可用 URL 时显示不可点击状态。

#### B. 友社推荐

- 仅在存在未过期友社活动时显示；
- 展示友社名称、活动标题、时间地点、完整简介、海报与详情外链；
- 过期友社活动直接从公开页面消失，不进入雨山前往期档案。

#### C. 往期活动

- 合并所有分类中状态为 `past` 的活动；
- 日期由新到旧排序；
- 分类按钮只显示实际含往期活动的分类；
- 桌面与移动端均采用海报 + 摘要行式布局；
- 点击海报或摘要切换详情；
- 详情完整显示 Notion 简介；
- 仅有真实 `reviewUrl` 时显示“查看活动回顾”。

**无障碍补充要求**：海报和摘要按钮已提供 `aria-expanded` 与 `aria-controls`；但折叠正文仍仅通过高度隐藏，后续应同步设置 `hidden`、`aria-hidden` 或等效语义，避免读屏器在收起状态仍读取全部正文。

### 4.4 支持页

#### A. 周边商品

当前仅为“Coming Soon”概念展示，包含帆布袋、书签、徽章和明信片四个固定占位项。

- 桌面端 Hover 显示对应图片占位；
- 移动端不展示预览图；
- 当前没有商品图片、库存、价格、购买链接或订单能力。

在真实商品上线前，本区不得被描述为可购买商店。

#### B. 活动票务

- 票务由未过期活动自动派生；
- 展示普通票与支持者票两档；
- 两档目前共用同一个 Eventbrite 报名 URL；
- 支持者票可配置价格和权益说明；
- 无票务时显示空状态。

当前票务卡始终渲染可点击 `<a>`；若有价格但缺少报名 URL，数据层会回退为 `#`。上线前必须改为不可点击状态或确保内容校验阻止该数据进入公开页；价格为 `0` 时当前也会显示为“—”，免费票场景需另行确认。

当前唯一票务记录是测试活动，不能用于正式上线。

#### C. 捐赠

`DonateSection` 组件仍存在但未被支持页挂载；Footer 已不再展示 Donate CTA，因此公开页面没有失效的 `/support#donate` 入口。当前产品状态是“捐赠未上线且入口隐藏”。

若后续上线捐赠，必须同时接入真实捐赠区块与平台，不得只恢复入口。

需要业务确认：收款平台、币种、建议档位、用途说明、税务或收据说明、隐私政策和成功反馈。

### 4.5 订阅 Modal

**触发入口**：

- 桌面导航订阅按钮；
- 移动菜单订阅按钮；
- 首页 Hero“加入我们”。

**当前交互**：

- 明信片视觉；
- 打开后锁定背景滚动并聚焦邮箱输入；
- Tab 焦点限制在弹窗内；
- 支持遮罩、关闭按钮与 Escape；
- 关闭后恢复原触发元素焦点；
- 提交中禁用输入和按钮；
- 成功后显示确认邮件提示；
- 未实现滚动 80% 自动弹出，也没有 Cookie 抑制逻辑。

**服务端流程**：

1. 标准化并校验邮箱；
2. 服务端调用 Mailchimp Marketing API；
3. 新联系人以 `pending` 状态写入 Audience；
4. 用户点击 Mailchimp 确认邮件后才成为 `subscribed`；
5. 已存在或等待确认的邮箱返回 409；
6. 配置缺失返回 503，外部失败返回 502。

上线前应补充基础频率限制，降低公开接口被批量滥用的风险。

### 4.6 Footer

- 采用 `Footer.module.css` 的合并式布局；
- 展示品牌名称、定位、Seattle 地址与页尾导航；
- 展示 Instagram、小红书、微信公众号外链；
- 小助手按钮打开二维码 Dialog，并提供微信外链；
- Footer 内嵌每日一句；无可用句子时整个每日一句区域不渲染；
- 客户端安全显示当前年份；
- 当前没有 Donate 或 Newsletter CTA。

上线前需逐一验证社交链接、微信二维码与每日一句降级状态。

---

## 5. 内容管理与业务规则

### 5.1 内容源优先级

```text
Notion 活动数据库
        │
        ├── 成功：原始记录 → 校验 → 持久化快照 → 页面标准化与状态计算
        │
        └── 请求失败：保留最后一次成功快照 → 页面继续展示
                         │
                         └── 从未成功同步：data/activities.json → 标准化 → 页面

Notion 海报文件 → 同步任务 → Cloudinary → 永久 URL 回写 Notion
Notion 每日一句数据库 → 启用项过滤 → 日期轮询 → 全局 Footer
```

### 5.2 活动状态规则

统一使用 `America/Los_Angeles` 计算活动归档时间。

| 原始状态或时间 | 公开结果 |
|---|---|
| 草稿、取消、隐藏及中英文别名 | 不公开 |
| 已完成、往期、归档及中英文别名 | `past` |
| 即将上线、筹备中及中英文别名 | `coming_soon` |
| 明确结束时间已过 | `past` |
| 无结束时间，开始日期次日零点已过 | `past` |
| 其他未过期状态 | `upcoming` |

### 5.3 Notion 活动字段

| 字段 | 类型 | 用途 |
|---|---|---|
| 活动名称 | Title | 必填中文标题 |
| 活动名称（英文） | Text | 可选英文标题 |
| 状态 | Select/Status | 即将举行、已完成、草稿等 |
| 分类 | Select | 活动主分类 |
| 活动类型 | Select | 细分类标签 |
| 开始时间 | Date | 日期与开始时间 |
| 结束时间 | Date | 可选；优先用于自动归档 |
| 地点 | Text | 城市或场地 |
| 地点详情 | Text | 详细地址或线上说明 |
| 简介 | Text | 中文正文唯一来源 |
| 英文简介 | Text | 可选英文正文 |
| 海报图片 | Files & media | 运营拖图入口 |
| 海报图片 URL | URL | Cloudinary 永久地址 |
| 海报图片同步标记 | Text | 同步指纹 |
| 报名链接（Eventbrite） | URL | 雨山前报名入口 |
| 友社链接 | URL | 友社详情入口 |
| 回顾文章链接 | URL | 真实回顾内容；有值才显示 |
| 普通票价 | Number | USD |
| 支持者票价 | Number | USD |
| 支持者票含周边说明 | Text | 支持者权益 |
| 是否置顶 | Checkbox | 同状态下优先排序 |
| 是否友社活动 | Checkbox | 进入友社推荐 |
| 友社名称 | Text | 友社来源 |
| 友社名称（英文） | Text | 可选英文来源 |

当前支持的标准分类：读书会、雨山前Talk、三小时线上阅读、写作营、剧本围读、亲子共读；另有多个中英文别名映射。未知分类会自动生成默认样式。

### 5.4 Notion 每日一句字段

| 字段 | 类型 | 用途 |
|---|---|---|
| 中文句子 | Title | 必填 |
| 英文翻译 | Text | 可选 |
| 作者 | Text | 署名 |
| 出处 | Text | 作品或来源 |
| 是否启用 | Checkbox | 关闭后不参与轮询 |

### 5.5 缓存与刷新

- 页面使用 Next.js Cache Components；活动页在请求时读取并标准化可跨实例、跨部署的 Data Cache 快照；
- 每日一句使用 `stale: 60s`、`revalidate: 600s`、`expire: 3600s`；活动快照自身每 600 秒重新验证；
- `/api/revalidate` 同时标记 `activities` 与 `sentences` 标签待重新验证；
- 海报同步成功后刷新 `activities` 标签；
- Vercel Cron 每 10 分钟调用 `/api/sync-images`；
- 活动快照只有在 Notion 返回结构有效且完整分页成功后才会写入；同步失败保留旧快照；
- 管理接口接受 `x-admin-key` 或 `Authorization: Bearer`；
- `ADMIN_PASSWORD` 与 `CRON_SECRET` 任一有效即可通过恒定时间比较鉴权。

### 5.6 静态兜底现状

以 2026-08-28 的时间规则计算，`data/activities.json` 当前降级结果为：

- 0 条近期活动；
- 10 条往期活动；
- 3 条无日期的 `coming_soon` 分类活动，但当前页面没有展示入口；
- 0 条公开友社活动；
- 0 条票务；
- 往期记录均无海报 URL，因此首页改用 8 张内置画廊海报。

该兜底只用于首次没有成功快照的初始化场景；已有成功快照时，外部服务暂时不可用不会切换到这份静态数据。

### 5.7 数据与同步边界

- 活动读取与海报同步均按 Notion 游标分页读取；
- 每日一句已实现分页；
- 活动快照的 Notion 查询设置超时并记录结构化成功/失败日志；
- 活动快照查询失败不会覆盖已有活动快照；
- 构建阶段不依赖 Notion，不会因上游失败阻塞构建；运行时无成功快照才显示初始化兜底，部署后由 Cron 继续同步最新内容。

---

## 6. 技术架构

### 6.1 技术栈

| 层级 | 当前选型 |
|---|---|
| 框架 | Next.js 16.2.6 App Router + Cache Components |
| UI | React 19.2.4 |
| 语言 | TypeScript 5.9，Strict Mode |
| 样式 | Tailwind CSS 4.3 + 全局 CSS + 组件内样式 |
| 内容源 | Notion API 2022-06-28 |
| 图片 | Cloudinary + 本地静态素材 |
| 订阅 | Mailchimp Marketing API |
| 部署目标 | Vercel |
| 动画 | React Hooks、CSS Transition/Keyframes、滚动进度计算 |

`framer-motion` 当前存在于依赖中，但源码未导入。上线前应移除未使用依赖，或在确有需求时明确使用范围。

活动快照仍使用 `unstable_cache`，以保留跨请求/部署缓存和后台刷新失败时继续提供旧快照的行为。Next.js 16.2.6 文档已将其标记为由 `use cache` 取代；迁移前必须先验证持久化与 stale-on-error 语义，不应直接机械替换。

### 6.2 API

| 接口 | 方法 | 鉴权 | 行为 |
|---|---|---|---|
| `/api/revalidate` | POST | Admin/Cron | 标记活动与句子缓存待重新验证 |
| `/api/sync-images` | GET/POST | Admin/Cron | 刷新活动快照并同步 Notion 海报至 Cloudinary |
| `/api/subscribe` | POST | 公开 | 校验邮箱并写入 Mailchimp `pending` |

### 6.3 环境变量

```text
NOTION_API_KEY
NOTION_ACTIVITY_DB_ID
NOTION_SENTENCE_DB_ID
ADMIN_PASSWORD
CLOUDINARY_CLOUD_NAME
CLOUDINARY_API_KEY
CLOUDINARY_API_SECRET
CLOUDINARY_FOLDER
CRON_SECRET
MAILCHIMP_API_KEY
MAILCHIMP_AUDIENCE_ID
MAILCHIMP_SERVER_PREFIX
```

所有密钥均为服务端变量，不得增加 `NEXT_PUBLIC_` 前缀或提交到 Git。

### 6.4 安全与隐私

已实现：

- 管理接口鉴权；
- 密钥仅在服务端读取；
- 管理密钥使用恒定时间比较；
- 邮箱只发送到 Mailchimp，不写入本地数据库；
- 外链使用 `noopener` / `noreferrer`；
- 邮箱长度和格式基础校验。

上线前补充：

- 订阅接口频率限制与异常流量监控；
- 隐私政策、邮件用途与退订说明；
- 第三方服务的数据处理说明；
- 定期轮换管理与 Cron 密钥；
- Cloudinary / Notion / Mailchimp 最小权限检查。

### 6.5 SEO 与可发现性

当前只有全局 `title`、`description` 与基础 Open Graph 文本。

上线前需补充：

- 每页独立 metadata；
- Open Graph 分享图；
- `sitemap.xml` 与 `robots.txt`；
- 正式 `metadataBase` 与 canonical URL；
- 结构化数据（Organization / Event，可作为上线后增强）；
- 中文与英文品牌关键词校对。

### 6.6 项目结构

```text
app/
  page.tsx                    首页服务端入口
  activities/page.tsx         活动页服务端入口
  support/page.tsx            支持页服务端入口
  api/revalidate/route.ts      缓存刷新
  api/sync-images/route.ts     海报同步
  api/subscribe/route.ts       Mailchimp 订阅

components/
  HeroRain.tsx                 当前编辑型 Hero（文件名为历史命名）
  OfferScrollList.tsx          “我们提供什么”滚动展开场景
  UpcomingShowcase.tsx         首页近期活动滚动卡片
  PhotoWall.tsx                往期海报画廊与详情 Dialog
  ActivitiesClient.tsx         活动单页流
  SupportClient.tsx            周边与票务
  SubscribeModal.tsx           全局订阅弹窗
  WechatAssistantButton.tsx    微信二维码入口
  SentenceOfDay.tsx            Footer 每日一句
  Footer.tsx/.module.css       合并式全局 Footer
  DonateSection.tsx            未挂载的捐赠占位组件

lib/
  content.ts                   内容统一出口与静态兜底
  notion.ts                    活动读取和标准化
  sentences.ts                 每日一句读取
  activityStatus.ts            状态与时区规则
  notionSync.ts                海报同步
  cloudinary.ts                Cloudinary 签名上传
  admin.ts                     管理接口鉴权

data/activities.json           静态兜底
vercel.json                    10 分钟图片同步 Cron
```

---

## 7. 视觉与体验规范

### 7.1 首页专属色板

| Token | 色值 | 用途 |
|---|---|---|
| Forest | `#1A241E` | Hero、导航与主要深色背景 |
| Paper | `#F3EBDD` | 首页纸张背景 |
| Rain | `#5B8790` | 蓝灰辅助色 |
| Vermilion | `#D85A3A` | CTA 圆形箭头 |
| Book | `#DDD06B` | 焦点和少量强调 |

### 7.2 内页与全局色板

| Token | 色值 | 用途 |
|---|---|---|
| Global Paper | `#FAF8F5` | 导航、活动页、支持页、Footer |
| Surface | `#FFFFFF` | 卡片与表单 |
| Ink | `#1C2220` | 主文字 |
| Forest | `#2E463D` | CTA、激活态、标签 |
| Muted | `#68736E` | 次要文字 |
| Sage | `#8FA499` | 英文标签与装饰 |
| Border | `#E6E2DA` | 分割线 |
| Note | `#F2EDE4` | 每日一句与占位背景 |
| Error | `#C0392B` | 表单错误 |

### 7.3 字体

| 用途 | 字体 |
|---|---|
| 全站标题、正文、英文展示与 UI | 本地 Chill Huo Song（常规 400、粗体 700、特粗 800） |
| 系统兜底 | `Songti SC`、`STSong`、`serif` |

当前 `--font-serif`、`--font-display`、`--font-sans`、`--font-label` 均指向同一套本地 Chill Huo Song 字体，不再使用 Noto Serif SC、Noto Sans SC、Playfair Display 或 Inter。

### 7.4 响应式与动态

- 核心移动验收宽度：375px；
- 主要断点：480px、639px、700px、900px；
- Hero 使用 `svh` 适配移动浏览器工具栏；
- Hero、服务列表、画廊和近期活动均提供减少动态模式；
- 所有弹窗应锁定背景滚动、支持 Escape 并恢复焦点；
- 纯 Hover 信息不得成为移动端获取内容的唯一方式。

---

## 8. 上线优先级与验收门槛

### P0 — 上线阻塞

- [ ] 删除或隐藏 3 条测试活动及全部 `example.com` 链接；
- [ ] 填入真实近期活动标题、海报、时间、地点、正文、报名链接和票价；
- [x] 暂时隐藏全部公开捐赠入口；恢复入口前仍需确认平台与业务规则；
- [ ] 使用真实测试邮箱完成 Mailchimp `pending → confirmation email → subscribed` 验收；
- [ ] 在 Vercel 配置全部环境变量和 Cron；
- [ ] 完成正式部署、域名、HTTPS 与线上桌面/移动回归；
- [ ] 验证 Notion、Cloudinary、Mailchimp 和微信所有真实外链；
- [ ] 更新静态兜底，使外部服务不可用时仍有有效近期内容与票务空状态；
- [ ] 清理提交范围，避免将 `.codex-tmp/`、`outputs/` 和临时预览文件误提交。

### P1 — 上线可靠性

- [x] 为活动查询与海报同步增加 Notion 分页；
- [x] 为 Notion 降级与同步失败增加结构化日志；
- [ ] 增加活动状态、内容标准化和 API 的自动化测试；
- [ ] 增加首页、活动页、订阅 Modal 的最小端到端回归；
- [ ] 为订阅接口增加频率限制；
- [ ] 确认桌面导航产品方案：维持 Logo + 订阅，或恢复核心页面链接；
- [ ] 为移动菜单增加 `aria-expanded`、`aria-controls` 与背景滚动锁定；
- [ ] 修正折叠活动正文的读屏语义；
- [ ] 票务 URL 缺失时禁用 CTA，并确认免费票的 `$0` 展示规则；
- [ ] 在减少动态偏好下停止社群照片自动轮播；
- [ ] 明确每日一句切换时区；
- [ ] 完成每页 SEO、OG 图片、Sitemap、Robots 与 canonical；
- [ ] 评估将活动快照从 `unstable_cache` 迁移到 `use cache`，并验证持久化与失败保旧语义；
- [ ] 移除未使用的 `framer-motion` 与未使用素材，或明确保留原因；
- [ ] 将 README 从脚手架说明更新为实际部署与运营手册。

### P2 — 上线后增长

- [ ] 定义并接入访问、活动 CTA、订阅和捐赠转化指标；
- [ ] 建立 Newsletter 模板、发送频率与内容流程；
- [ ] 上线真实周边商品与购买流程；
- [ ] 评估活动详情独立 URL 与分享能力；
- [ ] 评估站内搜索、活动分类落地页和结构化 Event 数据；
- [ ] 建立月度内容质量与链接有效性巡检。

### 8.1 最终上线验收清单

| 领域 | 验收标准 |
|---|---|
| 内容 | 页面无“测试”、占位价格、空海报或 `#` / `example.com` 业务链接 |
| 活动 | 近期、友社、往期、归档和分类规则与 Notion 一致 |
| 订阅 | 新邮箱收到确认邮件，重复邮箱得到合理提示 |
| 票务 | 两档票价、权益与 Eventbrite 页面一致 |
| 捐赠 | 当前保持入口完全隐藏；若恢复则必须可达真实能力 |
| 图片 | 海报同步、响应式加载与原图详情正常 |
| 缓存 | Notion 修改后 10 分钟内更新，手动刷新可用 |
| Cron | Vercel 定时同步成功且有失败记录 |
| 响应式 | 375px、768px、1440px 无溢出、遮挡或不可操作内容 |
| 无障碍 | 键盘可完成导航、订阅、Dialog 和活动详情操作 |
| SEO | 分享标题、描述、图片、Sitemap 和 Robots 正确 |
| 安全 | 密钥未暴露，管理接口拒绝未授权请求，订阅接口有限流 |
| 构建 | lint、TypeScript、生产构建与自动化回归全部通过 |
| 发布 | 工作区只包含预期文件，部署版本可回滚 |

---

## 9. 待业务确认

1. 正式域名与品牌英文写法是否最终确定；
2. 2026 秋季首个真实活动及友社推荐内容；
3. 普通票、支持者票的真实定价和权益；
4. 捐赠平台、用途、档位、收据与税务说明；
5. Newsletter 发件人、回复地址、频率和模板；
6. 每日一句按西雅图日期还是访问者本地日期切换；
7. 周边商品是否进入近期路线图；
8. 是否在上线首版接入基础分析工具与隐私政策。

---

*本 PRD 以 2026-08-28 `main@04d6483` 的代码、配置、静态数据、Notion 内容、生产构建和桌面/移动端浏览器验收结果为准。*
